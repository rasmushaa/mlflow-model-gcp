# Pipeline parameters
pipeline:
  text_vectorizer:
    features:
      - "receiver"
    transform_mode: "inplace"
    transform_suffix: "_vec"
    hyperparams:
      kbest: 500

  naive_bayes:
    features:
      - "*_vec"
    transform_mode: "inplace"
    transform_suffix: "_nb"
    hyperparams:

  xgboost:
    features:
      - "*_nb"
      - "amount"
    transform_mode: "replace"
    transform_suffix: ""
    hyperparams:
      random_state: 42

# Training parameters
training:
  target_column: "category"
  n_splits: 5
  shuffle: false

# Data parameters
query:
  start_date:
  end_date:
  mock: false

# Evaluation parameters
metrics:
  accuracy:
    reducers: ["mean", "minmax"]
  precision_macro:
    reducers: ["mean", "minmax"]
  recall_macro:
    reducers: ["mean", "minmax"]
  f1_score_macro:
    reducers: ["mean", "minmax"]
  roc_auc_macro:
    reducers: ["mean", "minmax"]
  roc_auc_micro:
    reducers: ["mean", "minmax"]
